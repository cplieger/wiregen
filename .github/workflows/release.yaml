name: Release
on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: cplieger/ci/.github/workflows/release.yaml@f024f691d9c2fcbaff3c74231a4cb25126630056 # v1
    with:
      target: go
