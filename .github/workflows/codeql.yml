name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  codeql:
    uses: cplieger/ci/.github/workflows/codeql.yaml@a28f3730fec44be2e29e687e779dd7d43520771c # v2
