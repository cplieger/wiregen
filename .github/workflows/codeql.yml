name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  codeql:
    uses: cplieger/ci/.github/workflows/codeql.yaml@469da1f600119cff4ed9b4674687cb5a4158b01f # v2
