name: Release
on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: cplieger/ci/.github/workflows/release.yaml@98c2f685fa19abbbc43735b1108bfd53d30e65e0 # v1
    with:
      target: go
