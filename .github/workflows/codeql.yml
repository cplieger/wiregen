name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  codeql:
    uses: cplieger/ci/.github/workflows/codeql.yaml@ba35d79d7edb7a6a566ec78a1d1fa4237e1f155f # v2
