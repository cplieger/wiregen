name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  codeql:
    uses: cplieger/ci/.github/workflows/codeql.yaml@2a1a6602f9d06ef47a10312e768ccf91cee2a545 # v2
